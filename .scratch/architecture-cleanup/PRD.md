# Architecture cleanup — PRD

Generated by `/improve-codebase-architecture` on 2026-05-11.

## Goal

Reduce architectural friction in ByteVim by deepening shallow modules, removing dead code, and fixing concrete bugs surfaced during a codebase walk. Vocabulary follows `CONTEXT.md`; load-bearing decisions are recorded in `docs/adr/`.

## Scope

Ten issues. Roughly grouped:

- **Layout / locality** — #01 split `editor.lua`, #07 consolidate git plugins, #03 move notes out of utils
- **Dead / duplicate code** — #06 delete dead extras, #08 drop duplicate auto-pairs, #10 resolve mason rename
- **Correctness bugs** — #04 `lsp_keymaps` ignores its args, #09 `coding.lua` hard-depends on tailwind extras
- **Bigger design decisions** — #05 fzf-lua vs telescope picker, #02 `bytevim.lang.define` helper

## Non-goals

- No plugin additions or feature work
- No rewriting of `lazy.nvim` / `mason.nvim` bootstrap
- No changes to keybindings beyond resolving collisions in #07

## Risks

- Most issues are local refactors; risk is low.
- #05 (picker) and #02 (lang helper) are larger and benefit from a grilling pass before being picked up.

## Status legend

See `docs/agents/triage-labels.md`.

- `ready-for-agent` — fully specified, an AFK agent can pick it up
- `ready-for-human` — needs a design call before implementation
- `needs-info` — waiting on the maintainer to resolve an open question
