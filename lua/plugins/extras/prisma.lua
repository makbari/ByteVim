return {
  prismals = {
    filetypes = { "prisma" },
    settings = {
      prisma = {
        prismaFmtBinPath = "prisma-fmt", -- Optional: Path to prisma-fmt binary if not in PATH
      },
    },
  },
}